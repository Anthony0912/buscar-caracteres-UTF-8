# Configuración antes de ejecutar el archivo app.py

## Para que identifique las variables de entorno debe instalar:
    - pip install python-decouple
## Luego debe importarlo:
    - from decouple import config